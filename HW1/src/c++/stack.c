#include "stack.h"
#include <stdlib.h>
/*
void Stack::push(int x)
{
    Node* node = (Stack::stack *)malloc(sizeof(Stack::stack));
    node->data = x;
    node->next = this->head;

    this->head = node;
};

int Stack::pop()
{
    int value = this->head->data;
    Node* tmp = this->head;
    this->head = this->head->next;

    free(tmp);

    return value;
};
*/

